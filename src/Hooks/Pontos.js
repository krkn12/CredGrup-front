export const calcularPontosDeposito = (valor) => {
  return 1; // 1 ponto por depósito, independente do valor
};

// Funções adicionais para cálculo de pontos
export const calcularPontosPagamento = (valor) => {
  return 1; // 1 ponto por pagamento
};

export const calcularPontosVenda = (valor) => {
  return 1; // 1 ponto por venda
};